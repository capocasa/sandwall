name: CI

on:
  push:
    branches: [main]
    tags: ['[0-9]+.[0-9]+.[0-9]+']
  pull_request:
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            artifact: sandwall-linux-amd64
            ext: tar.gz
          - os: macos-latest
            artifact: sandwall-macos-universal
            ext: tar.gz
          - os: windows-latest
            artifact: sandwall-windows-amd64
            ext: zip

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: jiro4989/setup-nim-action@v2
        with:
          nim-version: stable
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install dependencies
        run: nimble install -y --depsOnly

      - name: Build
        run: nim c --path:src -d:release -o:sandwall src/sandwall.nim

      - name: Build x86_64 and create universal binary (macOS)
        if: runner.os == 'macOS'
        run: |
          mv sandwall sandwall-arm64
          rm -rf ~/.cache/nim/sandwall_r
          nim c --path:src -d:release --passC:"-arch x86_64" --passL:"-arch x86_64" --cpu:amd64 -o:sandwall-x86_64 src/sandwall.nim
          lipo -create sandwall-arm64 sandwall-x86_64 -output sandwall
          file sandwall

      - name: Run tests
        run: |
          set -o pipefail
          nimble test 2>&1 | tee /tmp/sandwall-test.log
          # A nimble task abort has failed to fail this step before;
          # never report green with a FAILED test in the log.
          ! grep -q '\[FAILED\]' /tmp/sandwall-test.log

      - name: Package (Unix)
        if: matrix.ext == 'tar.gz'
        shell: bash
        run: |
          mkdir -p ${{ matrix.artifact }}
          cp sandwall ${{ matrix.artifact }}/
          cp README.md LICENSE ${{ matrix.artifact }}/
          tar czf ${{ matrix.artifact }}.${{ matrix.ext }} ${{ matrix.artifact }}

      - name: Package (Windows)
        if: matrix.ext == 'zip'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path ${{ matrix.artifact }} | Out-Null
          Copy-Item sandwall.exe ${{ matrix.artifact }}/
          Copy-Item README.md, LICENSE ${{ matrix.artifact }}/
          Compress-Archive -Path ${{ matrix.artifact }} -DestinationPath ${{ matrix.artifact }}.${{ matrix.ext }}

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}.${{ matrix.ext }}

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - name: Create release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create ${{ github.ref_name }} \
            --repo ${{ github.repository }} \
            --title "${{ github.ref_name }}" \
            --generate-notes \
            sandwall-linux-amd64.tar.gz \
            sandwall-macos-universal.tar.gz \
            sandwall-windows-amd64.zip
